import React from 'react';
import styled from 'styled-components';

const Intro = (props) =>
  <div>
    <h2>Kin Chan</h2>
    <p>The software engineer you've been searching for</p>
  </div>

export default Intro;